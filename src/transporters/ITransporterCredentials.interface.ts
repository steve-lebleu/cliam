/**
 * @description Define allowed credentials on a transporter configuration
 */
export interface ITransporterCredentials {
  apiKey?: string
  apiSecret?: string
  username?: string
  password?: string
}