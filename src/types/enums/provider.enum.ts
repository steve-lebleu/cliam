/**
 * @description Define supported web api providers
 */
export enum PROVIDER {
  sparkpost = 'sparkpost',
  sendgrid = 'sendgrid',
  brevo = 'brevo',
  mandrill = 'mandrill',
  mailgun = 'mailgun',
  mailjet = 'mailjet',
  postmark = 'postmark',
}